{
  "name": "sortsafe-tinyhomes",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "SortSafe Tiny Homes — comprehensive, filterable directory of real tiny houses",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "scrape": "node --experimental-strip-types scripts/scrape.ts",
    "consolidate": "node --experimental-strip-types scripts/consolidate.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
